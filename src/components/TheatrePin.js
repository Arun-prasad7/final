// React
var React = require('react')
var ReactDOM = require('react-dom')

// Google Maps
var ReactGMaps = require('react-gmaps')
var {Gmaps, Marker} = ReactGMaps

var TheatrePin = React.createClass({
  
  render: function() {
    return 
        
  }  
})

module.exports = TheatrePin;